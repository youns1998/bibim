#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <windows.h>
#include <locale.h>
#include <conio.h>
typedef struct
SetConsoleCursorPosition(GetStdHandle(STD_OUTPUT_HANDLE), cd);

printf("Hello World!");
scanf("%d",&num);
cout <<"Hello World!");
using namespace std;
for(i = 0; i < MAX_SIZE;i++)
switch (number)
arr = (int *)malloc(sizeof(int)*size);
if(i ==0) break;
do { printf("***");} while(i<10);


